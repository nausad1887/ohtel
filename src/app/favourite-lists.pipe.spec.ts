import { FavouriteListsPipe } from './favourite-lists.pipe';

describe('FavouriteListsPipe', () => {
  it('create an instance', () => {
    const pipe = new FavouriteListsPipe();
    expect(pipe).toBeTruthy();
  });
});
