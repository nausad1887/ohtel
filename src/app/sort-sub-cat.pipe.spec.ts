import { SortSubCatPipe } from './sort-sub-cat.pipe';

describe('SortSubCatPipe', () => {
  it('create an instance', () => {
    const pipe = new SortSubCatPipe();
    expect(pipe).toBeTruthy();
  });
});
